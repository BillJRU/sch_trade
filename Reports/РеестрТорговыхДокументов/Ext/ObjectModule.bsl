#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ОбработчикиСобытий

Процедура ПриКомпоновкеРезультата(ДокументРезультат, ДанныеРасшифровки, СтандартнаяОбработка)
	
	
	ПараметрДанных = КомпоновщикНастроек.Настройки.ПараметрыДанных.Элементы.Найти("Период");
	ПериодОтчета = КомпоновщикНастроек.ПользовательскиеНастройки.Элементы.Найти(ПараметрДанных.ИдентификаторПользовательскойНастройки);
	
	Если ПериодОтчета <> Неопределено Тогда
		ЗаголовокОтчета = НСтр("ru='Реестр торговых документов за';uk='Реєстр торгових документів за'") + " " + Формат(ПериодОтчета.Значение.ДатаНачала,"ДЛФ=D") + 
														" - " + Формат(ПериодОтчета.Значение.ДатаОкончания, "ДЛФ=D");
	Иначе
		ЗаголовокОтчета = НСтр("ru='Реестр торговых документов';uk='Реєстр торгових документів'");
	КонецЕсли;
	
	ПараметрЗаголовок = КомпоновщикНастроек.Настройки.ПараметрыВывода.Элементы.Найти("Заголовок");
	Если ПараметрЗаголовок <> Неопределено Тогда
		ПараметрЗаголовок.Значение = ЗаголовокОтчета;
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#КонецЕсли
